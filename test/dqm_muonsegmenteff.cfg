process RPCSegmentEff = {

     	untracked PSet maxEvents = {untracked int32 input = -1}
  
	source = PoolSource { 
   
	untracked vstring fileNames = {
#		"file:/afs/cern.ch/user/c/carrillo/scratch0/muon_100pt_S4_W0_.root"
		"file:/afs/cern.ch/user/c/carrillo/scratch0/SingleMuon100ptDigisDTyCSCSeg.101_170pre8_.root"
	
		}
	} 

	#keep the logging output to a nice level
	service = MessageLogger {}

	module museg = MuonSegmentEff { 

		untracked bool inclcsc = 1
		untracked bool incldtMB4 = 1
		untracked bool incldt = 1

		untracked double widestrip = 4
		untracked double widestripRB4 = 4
		untracked double MinCosAng = 0.99
		untracked double MaxD = 7.

		untracked string muonRPCDigis = "muonRPCDigis"
		untracked string cscSegments = "cscSegments"
		untracked string dt4DSegments = "dt4DSegments"

		untracked string rejected = "rejected.txt"
		untracked string rollseff = "rollseff.txt"
               
		untracked bool   EffSaveRootFile  = true
		untracked int32  EffSaveRootFileEventsInterval  = 100
		untracked string EffRootFileName  = "MuonSegEff_RPCGlobal.003.root"

	}

	include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
	include "Geometry/RPCGeometry/data/rpcGeometry.cfi"
	include "Geometry/CSCGeometry/data/cscGeometry.cfi"
	include "Geometry/DTGeometry/data/dtGeometry.cfi"
	include "Geometry/MuonNumbering/data/muonNumberingInitialization.cfi"

	#service = ModuleWebRegistry{ }
	service = DaqMonitorROOTBackEnd{}
	#service = LockService{untracked vstring labels = {"source"}}
	service = MonitorDaemon {
	       untracked   bool AutoInstantiate    = false
	       untracked  int32 reconnect_delay    = 5
	       untracked string DestinationAddress = "cmsdaquser0.cern.ch"
	       untracked  int32 SendPort           = 9090
	       untracked string NameAsSource       = "GlobalDQM"
	}



	module out = PoolOutputModule {
                untracked string fileName ="MuonSeg.root"
        }
	path p = {museg} 

}
