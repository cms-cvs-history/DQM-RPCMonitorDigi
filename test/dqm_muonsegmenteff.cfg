process RPCSegment = {

     untracked PSet maxEvents = {untracked int32 input = -1}
  
source = PoolSource 
{ 
     #replace 'myfile.root' with the source file you want to use
     #untracked vstring fileNames = {"file:sim_10muons_1-10GeV.root"} 
     untracked vstring fileNames = {"rfio:///castor/cern.ch/user/c/carrillo/MonteCarlo/ROOT/sim_10muons_1-10GeV.000.root"}
}

#keep the logging output to a nice level
service = MessageLogger {}

module demo = MuonSegmentEff { 

   bool inclcsc = false
   bool incldt = true
   int32 widestrip = 5
   string muonRPCDigis = "muonRPCDigis"
   string cscSegments = "cscSegments"
   string dt4DSegments = "dt4DSegments"
}

include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
include "Geometry/RPCGeometry/data/rpcGeometry.cfi"
include "Geometry/CSCGeometry/data/cscGeometry.cfi"
include "Geometry/DTGeometry/data/dtGeometry.cfi"
include "Geometry/MuonNumbering/data/muonNumberingInitialization.cfi"

path p = {demo} 

}
